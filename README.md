CREATE TABLE SUBJECTS(
    SUBJECT_ID INT,
    SUBJECT_NAME VARCHAR(30)
);

INSERT INTO SUBJECTS VALUES (1, 'MATHS');
INSERT INTO SUBJECTS VALUES (2, 'CHEMISTRY');
INSERT INTO SUBJECTS VALUES (3, 'PHYSICS');

CREATE TABLE STUDENTS(
    STUDENT_ID INT,
    SUBJECT_ID INT
);

INSERT INTO STUDENTS VALUES (10, 1);
INSERT INTO STUDENTS VALUES (20, 2);
INSERT INTO STUDENTS VALUES (30, NULL);

CREATE TABLE STUDENT_REC(
    SUBJECT_ID INT,
    STUDENT_ID INT,
    SUBJECT_NAME VARCHAR(30)
);


CREATE TABLE EMPLOYEE_DATA(
    EMP_ID INT,
    DEPT_ID INT,
    SALARY INT
);

INSERT INTO EMPLOYEE_DATA VALUES (1, 10, 25000);
INSERT INTO EMPLOYEE_DATA VALUES (2, 10, 15000);
INSERT INTO EMPLOYEE_DATA VALUES (3, 20, 25000);
INSERT INTO EMPLOYEE_DATA VALUES (4, 30, 50000);
INSERT INTO EMPLOYEE_DATA VALUES (5, 30, 10000);

CREATE TABLE EMPLOYEE_RANK(
    EMP_ID INT,
    DEPT_ID INT,
    SALARY INT,
    RANK INT
);


-- Exercises 
FOR 2
CREATE TABLE CUSTOMER(
CUSTOMER_ID INTEGER,
CUSTOMER_FIRST_NAME VARCHAR2(100),
CUSTOMER_LAST_NAME VARCHAR2(100),
EMAIL VARCHAR2(100),
GENDER VARCHAR2(10),
DOB DATE,
SSN VARCHAR2(100),
MOBILE VARCHAR2(100),
COUNTRY VARCHAR2(100),
ACCOUNT_NUMBER VARCHAR2(100),
ACCOUNT_OPENING_DATE DATE,
CURRENT_BALANCE DECIMAL(10,2)
)

TO_DATE(REPLACECHR(0, Date_of_Birth, '-', '/'), 'MM/DD/YYYY')
TO_DATE(REPLACECHR(0, Account_Opening_Date, '-', '/'), 'MM/DD/YYYY')
TO_DECIMAL(REPLACECHR(0, Current_Balance, '$', ''), 2)


-- Create Lookup Table
CREATE TABLE Regions (
    REGION_ID INT PRIMARY KEY,
    REGION_NAME VARCHAR(25)
);

-- Insert Sample Data into Lookup Table
INSERT INTO Regions (REGION_ID, REGION_NAME) VALUES 
(1, 'North America'),
(2, 'Europe'),
(3, 'Asia'),
(4, 'Middle East and Africa'),
(5, 'South America'),
(6, 'Oceania');

-- Create Source Table
CREATE TABLE Countries (
    COUNTRY_ID VARCHAR(10) PRIMARY KEY,
    COUNTRY_NAME VARCHAR(40),
    REGION_ID INT
);

-- Insert Sample Data into Source Table
INSERT INTO Countries (COUNTRY_ID, COUNTRY_NAME, REGION_ID) VALUES 
('US', 'United States of America', 1),
('CA', 'Canada', 1),
('UK', 'United Kingdom', 2),
('DE', 'Germany', 2),
('FR', 'France', 2),
('IN', 'India', 3),
('JP', 'Japan', 3),
('AE', 'United Arab Emirates', 4),
('ZA', 'South Africa', 4),
('BR', 'Brazil', 5),
('AU', 'Australia', 6),
-- INTENTIONAL MISMATCH FOR TESTING:
-- Region 99 does not exist in the Regions table. 
-- In Informatica, this should return a NULL REGION_NAME.
('AQ', 'Antarctica', 99);

-- Create Target Table (Empty, waiting for Informatica to load it)
CREATE TABLE COUNTRY_DATA (
    COUNTRY_ID VARCHAR(10),
    COUNTRY_NAME VARCHAR(40),
    REGION_ID INT,
    REGION_NAME VARCHAR(25)
);

-- Create the Departments Table (The Lookup Table)
CREATE TABLE Departments (
    DEPT_ID INT PRIMARY KEY,
    DEP_NAME VARCHAR(50),
    MANAGER_ID INT,
    LOCATION_ID INT
);

-- Insert data row by row, from your screenshot
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (10, 'Administration', 200, 1700);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (20, 'Marketing', 201, 1800);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (30, 'Purchasing', 114, 1700);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (40, 'Human Resources', 203, 2400);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (50, 'Shipping', 121, 1500);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (60, 'IT', 103, 1400);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (70, 'Public Relations', 204, 2700);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (80, 'Sales', 145, 2500);

-- **Extra Dummy Data for Testing**
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (90, 'Executive', 100, 1700);
INSERT INTO Departments (DEPT_ID, DEP_NAME, MANAGER_ID, LOCATION_ID) VALUES (100, 'Finance', 108, 1700);

-- Create the Employees Table (The Source Table)
CREATE TABLE Employees (
    EMP_ID INT PRIMARY KEY,
    FNAME VARCHAR(50),
    LNAME VARCHAR(50),
    EMAIL VARCHAR(50),
    PHN_NO VARCHAR(20),
    HIRE_DATE DATE,
    JOB_ID VARCHAR(20),
    SALARY INT,
    COMM_PCT DECIMAL(4, 2),
    MANAGER_ID INT,
    DEPT_ID INT
);

-- Insert data row by row, from your screenshot (Date formats corrected to MySQL YYYY-MM-DD)
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (105, 'David', 'Austin', 'DAUSTIN', '590.423.4569', '2005-06-25', 'IT_PROG', 4800, NULL, 103, 60);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (106, 'Valli', 'Pataballa', 'VPATABAL', '590.423.4560', '2006-02-05', 'IT_PROG', 4800, NULL, 103, 60);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (107, 'Diana', 'Lorentz', 'DLORENTZ', '590.423.5567', '2007-02-07', 'IT_PROG', 4200, NULL, 103, 60);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (108, 'Nancy', 'Greenberg', 'NGREENBE', '515.124.4569', '2002-08-17', 'FI_MGR', 12008, NULL, 101, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (109, 'Daniel', 'Faviet', 'DFAVIET', '515.124.4169', '2002-08-16', 'FI_ACCOUNT', 9000, NULL, 108, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (110, 'John', 'Chen', 'JCHEN', '515.124.4269', '2005-09-28', 'FI_ACCOUNT', 8200, NULL, 108, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (111, 'Ismael', 'Sciarra', 'ISCIARRA', '515.124.4369', '2005-09-30', 'FI_ACCOUNT', 7700, NULL, 108, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (112, 'Jose Manuel', 'Urman', 'JMURMAN', '515.124.4469', '2006-03-07', 'FI_ACCOUNT', 7800, NULL, 108, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (113, 'Luis', 'Popp', 'LPOPP', '515.124.4567', '2007-12-07', 'FI_ACCOUNT', 6900, NULL, 108, 100);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (114, 'Den', 'Raphaely', 'DRAPHEAL', '515.127.4561', '2002-12-07', 'PU_MAN', 11000, NULL, 100, 30);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (115, 'Alexander', 'Khoo', 'AKHOO', '515.127.4562', '2003-05-18', 'PU_CLERK', 3100, NULL, 114, 30);

-- **Extra Dummy Data for testing COMMISSION_PCT handling**
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (116, 'Shelli', 'Baida', 'SBAIDA', '515.127.4563', '2005-12-24', 'PU_CLERK', 2900, 0.10, 114, 30);
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (117, 'Sigal', 'Tobias', 'STOBIAS', '515.127.4564', '2005-07-24', 'PU_CLERK', 2800, 0.10, 114, 30);

-- **CRITICAL TEST CASE: Intentional mismatch**
-- This employee has DEPT_ID = 99. Dept 99 does not exist in the Departments table.
-- Your lookup must handle this and return a NULL for the department name.
INSERT INTO Employees (EMP_ID, FNAME, LNAME, EMAIL, PHN_NO, HIRE_DATE, JOB_ID, SALARY, COMM_PCT, MANAGER_ID, DEPT_ID) VALUES (999, 'Test', 'User', 'TUSER', '555.555.5555', '2026-01-01', 'IT_PROG', 5000, NULL, 103, 99);

-- Create the Target Table (Loaded by Informatica)
CREATE TABLE EMPLOYEE_DEPT (
    EMP_ID INT,
    FIRST_NAME VARCHAR(20),
    LAST_NAME VARCHAR(20),
    EMAIL VARCHAR(25),
    PHN_NO VARCHAR(20),
    HIRE_DATE DATE,
    JOB_ID VARCHAR(10),
    SALARY INT,
    COMMISSION_PCT INT,
    MANAGER_ID INT,
    DEPT_ID INT,
    DEPT_NAME VARCHAR(30)
);
